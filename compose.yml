services:
  ###################################################
  # Service: backend (FastAPI)
  ###################################################
  backend:
    container_name: job-search-agent-backend
    build:
      context: .
      dockerfile: Dockerfile
      target: backend-dev
    volumes:
      # Hot-reload: mount local code into container
      - ./app:/app/backend/app
    env_file:
      - .env
    environment:
      # In Docker, mysql service is reached by container name
      - DB_HOST=${DB_HOST:-mysql}
      - DB_PORT=${DB_PORT:-3306}
      - DB_NAME=${DB_NAME}
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - CORS_ORIGINS=${CORS_ORIGINS:-http://localhost}
      - SCRAPER_SERVICE_URL=${SCRAPER_SERVICE_URL:-http://job-search-agent-scraper:9000}
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.job-search-agent-api.rule=Host(`job-search-agent.local`) && PathPrefix(`/api`)"
      - "traefik.http.services.job-search-agent-api.loadbalancer.server.port=8000"
      - "traefik.http.routers.job-search-agent-api.entrypoints=web"
    expose:
      - 8000
    networks:
      - traefik-network
    restart: unless-stopped

###################################################
# Networks
###################################################
networks:
  traefik-network:
    external: true